<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

    <nav class="bg-blue-500 p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-white text-4xl font-bold">Blogs</h1>
        </div>
    </nav>

    <div class="container mx-auto py-8">
        <h2 class="text-5xl font-semibold mb-8 text-center">Latest Blogs</h2>

        <div class="grid grid-cols-1 md:grid-cols-1 lg:grid-cols-1 gap-8">
            <% blogs.forEach(blog => { %>
                <div class="w-2/3 mx-auto bg-white rounded-lg shadow-md p-6">
                    <img src="<%= blog.image %>" alt="Blog Image" class="w-full h-56 object-cover rounded-md mb-4">

                    <h3 class="text-xl font-semibold mb-2"><%= blog.heading %></h3>
                    <p class="text-gray-700 mb-4"><%= blog.content.substring(0, 100) %>...</p>

                    <div class="flex items-center justify-between text-gray-500">
                        <p><%= blog.designation %></p>
                        <% if (blog.createdAt) { %>
                            <p><%= blog.createdAt.toDateString() %></p>
                        <% } else { %>
                            <p>Unknown Date</p>
                        <% } %>
                    </div>

                    <div class="flex items-center mt-4 space-x-4">
                        <a href="whatsapp://send?text=<%= encodeURIComponent(blog.heading + ' - ' + blog.shareableLink) %>" class="text-green-500 hover:text-green-700" title="Share on WhatsApp" target="_blank" rel="noopener noreferrer">WhatsApp</a>
                        <a href="https://www.facebook.com/sharer/sharer.php?u=<%= encodeURIComponent(blog.shareableLink) %>" class="text-blue-500 hover:text-blue-700" title="Share on Facebook" target="_blank" rel="noopener noreferrer">Facebook</a>
                    </div>

                    <a href="/kisaan/blogs/<%= blog._id %>" class="block text-center text-blue-500 mt-4 hover:text-blue-700">See More</a>
                </div>
            <% }) %>
        </div>
    </div>

</body>
</html>
